import React, { Component } from 'react'
import {NavContainer, Logo} from "./styled"

export default class Nav extends Component {
    render() {
        return (
            <NavContainer>
                <Logo>GITHUB PROFILES</Logo>
            </NavContainer>
        )
    }
}
