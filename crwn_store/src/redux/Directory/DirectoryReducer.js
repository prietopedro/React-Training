import {data} from "../../directory.data"

const initialState = {
    sections: data
}

export const directoryReducer = (state = initialState, action) => {
    switch(action.type){

        default:
            return state
    }
}

